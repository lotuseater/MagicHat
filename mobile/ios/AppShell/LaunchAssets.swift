import Foundation

public enum LaunchAssets {
    public static let appDisplayName = "MagicHat"
    public static let launchHeadline = "Phone Control for Team App"
    public static let launchSubtitle = "Pair, monitor, prompt, and restore live PC task sessions."
}
